[project]
name = "topo-formatter"
version = "0.1.0"
description = "analysis.json → human/LLM-readable output"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/topo_formatter"]
