[project]
name = "topo-core"
version = "0.1.0"
description = "Computational core for topo structural analysis (Rust via PyO3)"
requires-python = ">=3.11"

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
features = ["python"]
