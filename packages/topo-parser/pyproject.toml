[project]
name = "topo-parser"
version = "0.1.0"
description = "Source code → typed multilayer graph"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/topo_parser"]
