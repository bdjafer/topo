[package]
name = "topo-cache"
version = "0.1.0"
edition = "2021"
description = "Content-addressed cache for topo parse results"

[lib]
name = "topo_cache"

[dependencies]
blake3 = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
